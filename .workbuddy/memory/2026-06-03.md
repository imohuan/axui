# 2026-06-03

## AxButton 修复

- 将加载状态内置到 `AxButton` 组件，新增 `loading` prop，自动渲染旋转 spinner。
- 修复 `size="icon" / "icon-lg"` 时图标未居中的问题：
  - 新增 `isIconOnly` 计算属性，在 icon-only 模式下自动隐藏默认 slot 文本，避免图标+文字挤在 24px 按钮内导致偏移。
  - 为图标 span 和 loading spinner 添加 `leading-none`，消除字体行高造成的细微偏移。
  - 预览区示例改为使用 `icon` prop 而非 slot 传入图标，确保走组件内部居中逻辑。
- `ComponentsView.vue` 中按钮示例简化，不再在 slot 里手写 spinner SVG。

## 控件高度体系新增 xl 档 (32px)
- 新增 `xl` 到 ButtonSize/InputSize/ControlSize
- SIZE_CLASSES：xl = `h-8 px-3.5 py-2`（所有组件一致）
- Switch：xl track `h-8 w-12`，thumb `h-6 w-6`，translate `translate-x-5`
- ComponentsView：Button 加 xl 在 lg 和 icon 之间，Input/Switch/Select 加 xl 在 lg 之后
- ui-style.md §7.1/§8.5/§12 同步
- 构建通过

## docs 文档同步 xl 尺寸
- `src/components/ui/docs/ui-style.md`：§2.1 高度基准表替换为完整 xs/sm/md/lg/xl 表格；§2.2 宽度约束 Input 描述更新
- `src/components/ui/docs/components.md`：Button/Input/Select/Switch 的 size 类型引用全部补上 `xl`；icon 按钮注释尺寸更新（24px/32px）
- 构建通过
